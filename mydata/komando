cd /mydata/llm/vector
docker build -t vector .

docker run -it --rm \
  -p 8888:8888 \
  -v /mydata/llm/vector:/mydata/llm/vector \
  llm-vector:latest


  # コンテナ内に入る（例：vector）
docker exec -it vector bash

python3 /mydata/llm/vector/script/run_all_pipeline.py
python3 /mydata/llm/vector/script/debugvs.py "公職選挙法の一票の格差"

# Jupyter を再起動（例：vector）
docker restart vector

# FastAPI のログ確認
docker logs -f fastapi

2. 既存マウントを一旦解除（安全のため）
bash
コードをコピーする
sudo umount /mydata/nas
sudo umount /mydata/nas_all


3. fstabを適用（テストマウント）
bash
コードをコピーする
sudo mount -a


